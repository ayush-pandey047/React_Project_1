import React from "react";

const ContactList = (props) => {
    console.log(props)
    return(
        <div className="ui cellied list">
            Contact List
        </div>
    )
}

export default ContactList;